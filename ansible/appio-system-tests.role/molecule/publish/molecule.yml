---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: test-appio-publish
    image: talsenteam/docker-ci-image-dotnet-core-runtime:v2.1
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: publish
  test_sequence:
    - lint
    - syntax
    - destroy
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - destroy
verifier:
  name: testinfra
  lint:
    name: flake8
